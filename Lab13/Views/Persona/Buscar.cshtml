@model List<Lab13.Models.Persona>


@using (Html.BeginForm("Buscar", "Persona", FormMethod.Get))
{

    <h2>Buscar Personas</h2>

    <div class="row">
        <div class="col-md-1">
            <label>Criterio</label>
        </div>
        <div class="col-md-2">
            @Html.TextBox("criterio")
        </div>
        <div class="col-md-2">
            <input class="btn btn-success" type="submit" value="Buscar" />
        </div>
    </div>
}


@{
    ViewBag.Title = "Listar";
    var grid = new WebGrid(source: Model, columnNames: new[]{
        "PersonaID","Nombre","Apellido","FechaNac","Direccion","Email"
        });
}



@grid.GetHtml(tableStyle: "table table-striped",
    columns: grid.Columns(
        grid.Column(columnName: "PersonaID", header: "PersonaID"),
        grid.Column(columnName: "Nombre", header: "Nombre"),
        grid.Column(columnName: "Apellido", header: "Apellido"),
        grid.Column(columnName: "FechaNac", header: "FechaNac"),
        grid.Column(columnName: "Direccion", header: "Direccion"),
        grid.Column(columnName: "Email", header: "Email"),
        grid.Column(columnName: "Acciones", format: (item) =>
                Html.ActionLink("Ver", "Mostrar", "Persona",
                    new { id = item.PersonaID }, null))
        )
    )


<!--
<table class="table table-striped">
    <tr>
        <th>PersonaID</th>
        <th>Nombre</th>
        <th>Apellido</th>
        <th>Fecha</th>
        <th>Dirección</th>
        <th>Email</th>
        <th>Acciones</th>
    </tr>
    @foreach (var persona in Model)
    {
<tr>
    <td>@persona.PersonaID</td>
    <td>@persona.Nombre</td>
    <td>@persona.Apellido</td>
    <td>@persona.FechaNac</td>
    <td>@persona.Direccion</td>
    <td>@persona.Email</td>
    <td>@Html.ActionLink("ver","Mostrar","Persona",
       new { id=persona.PersonaID},null)
    </td>
</tr>
    }
</table>

-->
